{
  "$schema": "https://opencode.ai/config.json",
  "skills": {
    "paths": [".opencode/skills", "OPENCODE>SKILL"]
  },
  "references": {
    "stripe": {
      "repository": "stripe/stripe-node",
      "description": "Use for Stripe API integration, checkout sessions, webhooks, and subscription management"
    }
  }
}
